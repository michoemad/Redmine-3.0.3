<% content = render :parent%>
<% html = Nokogiri::HTML.fragment(content) %>
<% k = html.at_css("select#issue_assigned_to_id")%>
<% k.add_next_sibling(capture do %>
<br><p>
<label><%=l(:field_percentage) %></label> <%=select_tag "issue[percentage]", options_for_select((0..10).to_a.collect {|r| ["#{r*10} %", r*10.0] },@issue.percentage*100.0) %>
</p><p>

 </p>

<% end) %>

<%=raw html %>